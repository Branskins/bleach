# sliding_window

Solved problems using the [sliding-window skill](../../.claude/skills/sliding-window/SKILL.md).

One solution file + one `test_*.py` per problem, snake_case named after the problem.
