# Architecture

System design and architecture learnings that come up while preparing for screenings —
not tied to a specific coding pattern, so kept as notes rather than a Claude Code skill.

Add one markdown file per topic as it's studied (e.g. `caching.md`, `rate_limiting.md`,
`load_balancing.md`, `database_scaling.md`).
